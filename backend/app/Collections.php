<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Collections extends Model
{

    protected $fillable = [
        'code_collection', 'name', 'code_team', 'status'
    ];

    protected $table = 'collections';
    protected $primaryKey = 'code_collection';
    public $timestamps = false;
    public $incrementing = false;

    public function toArray()
    {
        $arr = parent::toArray(); // TODO: Change the autogenerated stub
        $arr['team'] = $this->team;
        $count = Examinations::where('code_collection', $arr['code_collection'])->count();
        $arr['number_of_examination'] = $count;
        return $arr;
    }

    public function team()
    {
        return $this->belongsTo('App\\Teams', 'code_team');
    }
}
